class Point {
  List<double> position;

  double distanceToCursor;

  double x;

  double y;

  int distanceToCenter;

  Point(this.position, this.distanceToCursor, this.x, this.y, this.distanceToCenter);
}